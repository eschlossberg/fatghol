#. Riscrivere `.isomorphisms_to()`:

     - semplificare la generazione della mappa di vertici in vertici;

     - eliminare l'uso dei "repetition_pattern": il numero di loop
       insistenti su un vertice è più semplice da calcolare ed è
       sicuramente un invariante altrettanto buono.

#. Inserire esempio dei grafi che fallivano l'equivalenza nei
   doctest di __eq__.

#. Usare ``rational.py`` in luogo di NZMATH.

#. Trasformare `.numbering` in una `property`.  Usare la forma
   accettata da `.numbering_set()` anche nei doctest e rimuovere il
   test `isinstance` in `Graph.__init__`.



DONE
====

#. Rimuovere ogni riferimento a `edge_seq`.

#. Rinominare `*_factory` in `*type`: `vertex_factory` -> `vertextype`



ABANDONED
=========

#. Completare il meccanismo di tracciamento delle contrazioni dei
   grafi, in modo da ridurre il numero delle comparazioni.

   -- bisogna tenere traccia di questa struttura in `graph_homology.py`,
      non aggiungere tutta questa roba in `Graph`

#. Usare `kwargs.pop` invece di `kwargs.get`; rimuoverlo del tutto per
   i parametri non pubblici.

